# Server-only security configuration (non-secret, but integrity-sensitive).
AUTH0_AUDIENCE=
AUTH0_ISSUER=
CORS_ORIGINS=

# Server-only secret. Must be the least-privilege steady_runtime URL.
DATABASE_URL=

# Non-secret operational configuration.
DEPLOYMENT_VERSION=
HOST=
PORT=
