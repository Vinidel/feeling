# Non-secret configuration contract. Store populated values in the selected
# deployment provider's secret manager; never commit them.
DATABASE_URL=
MIGRATION_DATABASE_URL=
DATABASE_SSL_ROOT_CERT_PATH=
SUPABASE_PROJECT_REF=
